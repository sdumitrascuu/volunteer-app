import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        primary: { main: '#3AB795'},
        secondary: { main: '#F76C5E'},
        warning: { main: '#FFD9762'},
    },
    typography:{
        fontFamily: '"Roboto", sans-serif',
    },
});